import React from 'react'

const Title = ({ title }) => (
  <header className="single-project_header">
    <h1 className="single-project__title">{ title.text }</h1>
  </header>
)

export default Title
